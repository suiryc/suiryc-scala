# Notes:
# Some unit tests may rely on Configuration handling.
# To prevent issues relatively to the default application configuration, use a
# unique filename here and ensure (sbt and unit tests setup) it is used over
# the default.
